---
layout: layouts/base
---

{% set postNum = page.url | postNumber(collections.all) %}
{% set displayTags = (tags or []) | filterTagList %}

<article class="h-entry" data-type="quote" data-pagefind-body>
	{% if image %}
		<img src="{{ image }}" alt="" class="list-card-image"/>
	{% endif %}

	<div class="post-meta">
		<p>
			{% if postNum %}<span class="post-number">#{{ postNum }}</span>{% endif %}
			<span class="type">Quote</span>
			<span class="sep">·</span>
			<time class="dt-published" datetime="{{ date }}">{{ date | displayDate }}</time>
		</p>
	</div>

	<div class="content">
		{{ content | safe }}
	</div>

	{% if displayTags.length %}
	<div class="post-tags">
		{% for tag in displayTags %}
		<a class="post-tag" href="/tags/{{ tag | tagSlug }}/">{{ tag }}</a>
		{% endfor %}
	</div>
	{% endif %}

	{% include "partials/post-footer.njk" %}
</article>

{% include "partials/transmit.njk" %}
